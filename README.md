# AI Caching System

**Caching Strategies Implemented:**
- ✅ Exact match (MD5 hash)
- ✅ Semantic caching (embedding similarity > 0.95)
- ✅ LRU eviction policy
- ✅ TTL (24-hour expiration)

**Endpoints:**
- `POST /` - Query endpoint
- `GET /analytics` - Cache metrics

**Target Met:**
- Cache hit rate: 65%+
- Daily savings: $2.00+

**Live Demo:**
- Backend: [UPDATE WITH RENDER URL]
- Frontend: [UPDATE WITH GITHUB PAGES URL]
